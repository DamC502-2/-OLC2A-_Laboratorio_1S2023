#include "simbolo.h"

Simbolo::Simbolo(Resultado *resultado, Tipo *tipo, const QString &nombre) : resultado(resultado), tipo(tipo),
                                                                            nombre(nombre) {}
